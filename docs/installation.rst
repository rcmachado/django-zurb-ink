============
Installation
============

At the command line::

    $ easy_install django-zurb-ink

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-zurb-ink
    $ pip install django-zurb-ink
